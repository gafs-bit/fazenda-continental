# OS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.venv-mcp/
.venv-bot/
venv/

# Telegram bot secrets (token, allowlist) — never commit
telegram_bot/.env

# Data (raw/sensitive farm data — keep local unless you decide otherwise)
data/*
!data/.gitkeep

# Logs (same PII sensitivity as data/ -- can contain driver names, romaneio numbers)
logs/

# Editors
.vscode/
.idea/
